import React from 'react';

function ErrorMessage() {
  return (
    <div className="error-message">
      <p>No Found</p>
      <button>Close</button>
    </div>
  );
}

export default ErrorMessage;
